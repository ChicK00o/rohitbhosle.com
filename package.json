{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "publish": "git subtree push --prefix .output/public origin nuxt-publish"
  },
  "devDependencies": {
    "nuxt": "3.0.0"
  }
}
